# No third-party dependencies — the server uses only the Python standard
# library (socket, threading, hashlib, base64, struct, json). This file is
# intentionally (almost) empty so hosts like Render still detect a Python app.
